import { ConfigInterface } from '@/interfaces/config.interface'

const config: ConfigInterface = {
  general: {
    name: 'Brain Arts',
    domain: 'nft.BrainArts.com',
    twitterHandle: '@BrainArts',
  },
}

export default config
